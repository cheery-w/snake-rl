"""
全局配置文件
所有超参数集中管理，方便调优
"""


class Config:
    # ---------------------------------------------------------------
    # 游戏设置
    # ---------------------------------------------------------------
    GRID_COLS   = 20          # 网格列数
    GRID_ROWS   = 20          # 网格行数
    CELL_SIZE   = 20          # 每格像素大小
    WINDOW_W    = GRID_COLS * CELL_SIZE   # 400
    WINDOW_H    = GRID_ROWS * CELL_SIZE   # 400
    FPS         = 20          # 可视化帧率

    # ---------------------------------------------------------------
    # 状态 & 动作空间
    # ---------------------------------------------------------------
    # 11 维向量状态:
    #   [危险直/右/左, 方向左/右/上/下, 食物左/右/上/下]
    STATE_SIZE  = 11
    ACTION_SIZE = 3           # 0=直走  1=右转  2=左转

    # ---------------------------------------------------------------
    # 奖励设计
    # ---------------------------------------------------------------
    REWARD_FOOD    =  10.0   # 吃到食物
    REWARD_DEATH   = -10.0   # 死亡
    REWARD_CLOSER  =   0.1   # 向食物靠近
    REWARD_FARTHER =  -0.1   # 远离食物

    # ---------------------------------------------------------------
    # 网络结构
    # ---------------------------------------------------------------
    HIDDEN_SIZES = [256, 256]  # 隐藏层神经元数
    USE_DUELING  = True        # 是否使用 Dueling DQN
    USE_DOUBLE   = True        # 是否使用 Double DQN

    # ---------------------------------------------------------------
    # 训练超参数
    # ---------------------------------------------------------------
    LR              = 1e-3       # Adam 学习率
    GAMMA           = 0.99       # 折扣因子
    BATCH_SIZE      = 64         # 每次学习的批大小
    MEMORY_SIZE     = 100_000    # 经验回放缓冲区容量
    WARMUP_STEPS    = 1_000      # 开始学习前的随机探索步数
    TARGET_UPDATE   = 1_000      # 目标网络硬更新间隔（步）
    NUM_EPISODES    = 2_000      # 总训练轮数
    MAX_STEPS_EP    = 10_000     # 每轮最大步数（防止无限循环）

    # ε-贪心衰减（乘法衰减，每轮结束后）
    EPS_START = 1.00
    EPS_END   = 0.01
    EPS_DECAY = 0.995

    # ---------------------------------------------------------------
    # 保存 & 日志
    # ---------------------------------------------------------------
    CHECKPOINT_DIR = "checkpoints"
    LOG_DIR        = "logs"
    SAVE_FREQ      = 100   # 每 N 轮保存一次 checkpoint
    EVAL_FREQ      = 50    # 每 N 轮做一次评估
    EVAL_EPISODES  = 10    # 评估时运行的轮数

    # ---------------------------------------------------------------
    # 随机种子
    # ---------------------------------------------------------------
    SEED = 42
